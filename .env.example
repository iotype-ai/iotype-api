# Copy to .env and fill in. .env is gitignored — never commit a real token.
# Generate a token at https://iotype.com/api-service/authentication
IOTYPE_TOKEN=

# Optional override, defaults to https://iotype.com
# IOTYPE_BASE_URL=https://iotype.com
